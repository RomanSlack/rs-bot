[project]
name = "rsbot"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mujoco>=3.10.0",
    "numpy>=2.5.1",
    "pillow>=12.3.0",
]

[dependency-groups]
dev = [
    "build123d>=0.11.1",
    "pytest>=9.1.1",
]
